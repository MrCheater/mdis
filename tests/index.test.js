const fs = require('fs');
const path = require('path');

test('named-block should works correctly', async () => {
  // console.log(
  // 	await parse(
  // 		path.join(
  // 			__dirname,
  // 			'named-blocks.js'
  // 		)
  // 	)
  // )
});
